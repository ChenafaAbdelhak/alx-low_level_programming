#include "main.h"
/**
 * _strspn - search prefix
 * @s: string
 * @accept: prefix str
 * Return: nbr of matches
 */
unsigned int _strspn(char *s, char *accept)
{
	int nbr_match = 0;
	return (nbr_match);
}
